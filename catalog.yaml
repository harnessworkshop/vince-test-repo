apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: vince-test-repo
  description: A test repo
  annotations:
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: group:default/teama
  lifecycle: experimental
